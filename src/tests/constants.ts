import { join } from "path";

const PROJECT_ROOT = join(__dirname, "../..");

export const IMAGE_PATH = join(PROJECT_ROOT, "src/tests/fixtures/stitch.jpg");
export const UPLOAD_PATH = join(PROJECT_ROOT, "uploads");
export const THUMBNAIL_PATH = join(PROJECT_ROOT, "thumbnails");

import { join } from "path";

export const IMAGE_PATH = join(__dirname, "../fixtures/stitch.jpg");
export const UPLOAD_PATH = join(__dirname, "../../uploads");
export const THUMBNAIL_PATH = join(__dirname, "../../thumbnails");
